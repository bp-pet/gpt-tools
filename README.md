## Tools for OpenAI tools like GPT and Dall-E

You need to put your GPT token in the .env file like 'GPT_TOKEN=xxx'.

### Dall-E
Open `dalle.py`, change parameters, run file.

### GPT
Run `gpt.py` and interact with the CLI.
